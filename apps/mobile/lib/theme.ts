// Single source of truth for design tokens used in JS contexts
// (Tailwind classes cover everything else — change colors in tailwind.config.js)
export const colors = {
  primary: "#1A56DB",
  muted: "#6B7280",
  danger: "#EF4444",
  success: "#22C55E",
  surface: "#F8FAFC",
} as const;
